describe("Compress Function", ()=>{
	it("should scan directories for lambda functions that match pattern", () => {
		
	});
	it("should create an archive for each lamda function found", () => {
		
	});
	it("should handle any errors", () => {
		
	});
});